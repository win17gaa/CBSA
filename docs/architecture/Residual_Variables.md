# Residual Variables

Residual variables capture unmodeled cognitive noise, uncertainty, and
non-deterministic behavior in the CBSA system.

They represent the portion of cognitive state that cannot be explained
by D(t), S(t), B(t), C(t), I(t), or V(t).

(Full detailed content will be added later.)
